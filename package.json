{
	"name": "line-translate-worker",
	"version": "0.0.0",
	"private": true,
	"description": "LINE bot translation worker powered by OpenAI and Cloudflare Workers.",
	"engines": {
		"node": ">=20.0.0"
	},
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.99.0"
	}
}
